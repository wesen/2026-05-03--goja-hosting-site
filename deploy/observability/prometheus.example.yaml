global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: goja-site
    metrics_path: /metrics
    static_configs:
      - targets:
          - 127.0.0.1:19090
        labels:
          environment: local-benchmark
